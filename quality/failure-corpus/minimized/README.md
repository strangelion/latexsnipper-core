# Minimized candidates

Each entry preserves the failure while removing unrelated AST subtrees or document content. Parser divergence includes the first structural path; Office entries retain only the minimal read-back payload.
